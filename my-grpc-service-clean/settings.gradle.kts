rootProject.name = "my-grpc-service-clean"

pluginManagement {
    repositories {
        gradlePluginPortal() // Required for most Gradle plugins
        mavenCentral()       // Kotlin plugin is also available here
        google()             // Optional, but good to have
    }
}
